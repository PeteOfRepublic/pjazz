window.onload = function() {

  pjazz(); // default is document.body for all of these - if you have specific targets and valid IDs in mind then add them here. The order is links, target, source.
  // pjazz( "content", "content", "links"); // an example

}
